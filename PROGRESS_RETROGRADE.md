# 狼羊棋逆推求解器优化：进度报告

> 分支：`optimize/retrograde`
> 报告时间：2026-08-17（终版更新：2026-08-18）
> 状态：**全部完成、可交付**——k=4..7 结果码 100% 匹配原表库，DTM 距离经独立校验全部最优；k=8 无原表可比，独立校验同样通过。

---

## 1. 任务目标

用**逆推分析（retrograde analysis）**替代原 `solver.cpp` 的**并行价值迭代**：

1. 降低计算复杂度——每个状态、每条边只处理常数次，而不是全表反复扫描。
2. 充分利用 80 核设备资源——按 `wolf_rank` 分块并行，初始化与传播两阶段都并发。
3. 产出结果：**结果码 100% 正确** + **DTM 距离最优**，并快于原求解器。

---

## 2. 已完成内容

### 2.1 逆推求解器实现

新增文件：

| 文件 | 内容 |
|---|---|
| `include/solver_retro.h` | `WolfMove` / `RevWolfSimple` / `RetroMoveTable` / `RetrogradeSolver` 定义 |
| `src/solver_retro.cpp` | 逆推核心实现（约 720 行） |
| `tools/solve_retro.cpp` | 主入口（`--data-dir --threads --start-k --end-k`） |

算法要点：

- **64 个距离桶 BFS**：结果码 2 bit + 距离 6 bit（0–63）。
- **计数器 `cnt[s]`** = 非 LOSS 后继数量（UNKNOWN/DRAW）；后继解析为 LOSS 时原子递减，归零则该状态判 LOSS。
- **无分配生成器**：栈上 `int[22]` 数组 + `decode_combination` 直接解码 free_idx，避免每状态 `std::vector` 分配。
- **原子操作**：`mmap` 字节上用 `__atomic_*` 内置函数做 CAS / fetch_sub，支持多线程无锁传播。
- **两阶段初始化**：Phase 1（狼回合：终局/吃子胜/计数器）、Phase 2（羊回合：终局/胜负/计数器）。
- **逆推传播**：反向狼普通移动 + 反向羊移动，跨桶吃子直接读 k−1 表库。

### 2.2 构建集成

- `Makefile` 的 `solve_retro` 目标链接 `$(SOLVER_OBJ)`（解析 `MOVE_TABLE`）。
- `CMakeLists.txt` 的 `solve_retro` 链接 `solver`。

### 2.3 终局规则修正

原求解器把“三狼全被堵死 → 羊胜”同时标到狼回合和羊回合（位置属性，与回合无关）。逆推实现最初只在狼回合标记，已修正为**两个回合都标记**（`wolf_adj_free` + `all_blocked` 检查），终局数从 92 → 184。

### 2.4 移动生成一致性验证

`tools/diagnose3.cpp` 对比 `RetroMoveTable` 与 `board.h::gen_moves`：

- 狼**普通移动**：一致。
- 狼**吃子**：一致。
- 结论：移动生成无缺失、无多余（此前疑似问题实为诊断对比逻辑把吃子误算进普通移动集合，非求解器 bug）。

---

## 3. 性能结果（已达标）

| 桶 | 逆推（80 线程） | 逆推（1 线程） | 原价值迭代（80 线程） |
|---|---|---|---|
| k=4 | ~5s | ~23s | 101s |

- 逆推 k=4 约为原求解器的 **20 倍加速**。
- 理论复杂度为 O(状态数 × 平均分支因子)，优于原价值迭代的全表多轮扫描。

---

## 4. 已修复的原理性 bug（全部修复完毕）

> 本节保留原始 bug 记录作为追溯；所有问题均已定位根因并修复，验证见 §5。

### 4.1 历史 bug 一：250,156 个 DRAW 被误判为 WOLF_WIN

修复前对 k=4 全表逐状态比对（逆推 vs 原表库 `wsf_tb_dtc_k04.bin`）：

| 结果码 | 原表库 | 逆推（修复前） | 差异 |
|---|---|---|---|
| WOLF_WIN (0) | 33,398,108 | 33,648,272 | +250,164 |
| SHEEP_WIN (1) | 736 | 728 | −8 |
| DRAW (2) | 250,156 | **0** | −250,156 |

**根因**：`init_sheep_range` 在初始化计数器时**没有把已解析为 WOLF_WIN 的后继计入 `cnt`**。传播阶段这些后继被处理并各递减一次，导致计数“漏计”→ 提前归零 → 仍含 UNKNOWN（和棋逃生口）的状态被误判为 LOSS。

**修复**：羊回合 init 对 WOLF_WIN 与 UNKNOWN 后继**一律计数**（它们都会在传播中被处理并递减），同时删除“全部后继已胜则立即判负”的捷径——该捷径用 init 时的继承距离判负，而这些距离随后可能被胜线升级缩短，导致距离陈旧。现在一律走计数器 + 传播解析，距离必然基于最终距离。

### 4.2 历史 bug 二：k≥5 距离不是 DTM 最优（传播阶段缺失两个机制）

修复前 `check_tb` 报告 k=5 有 2.2M 距离错误（全部为距离偏大）。根因有二，均只在 k≥5 出现（k=4 的吃子落到平凡桶 k=3，init 吃子胜距离恒为 1，天然最优）：

1. **WIN 路径不能降级**：init 已按吃子胜赋过距离的状态，传播中发现更短的普通移动胜线时被 `result != UNKNOWN → continue` 阻挡。修复为**最小距离 CAS 升级循环**：同结果且距离更小则覆盖并重新入桶（配合距离守卫防重复处理）。
2. **LOSS 距离忽略跨桶捕获**：狼回合状态的吃子后继在 k−1 表，其最大吃子败距离（`max_loss`）不参与本桶计数，置零时刻的波次 `nd` 可能小于真实需要。新增全尺寸 `loss_dists_` 数组存 `max_loss`，LOSS 赋值距离取 `max(nd, loss_dists_[prev]+1)`。

### 4.3 历史 bug 三（k≥6 最后发现）：LOSS 路径“僵尸桶”条目

修复 bug 二后 k=6 出现 1,224 个内部结果不一致：**LOSS 路径按 `nd`（下一波）入桶，但实际赋的距离 `dist = max(nd, loss_dists_+1)` 可能大于 `nd`**。波次守卫 `tb_distance != wd` 会把这些条目永久跳过——状态在表里已赋值但**从不被处理**，其前驱永远收不到 WIN 传播（典型：`wr=0 sr=38` 被跳过 → 前驱 `sr=72` 本应 SHEEP_WIN@11 却留为 DRAW）。

**修复**：`propagate_range` 的输出改为 `(状态索引, 实际距离)` 对，调用方**按实际距离入桶**；WIN 路径距离恒为 `nd` 不变。

### 4.4 附带修复

- `tools/check_tb.cpp` 距离比较的原 `expected_d & 0x3F` 会在 64 处回绕（把期望 64 当 0）导致假阳性；改为**封顶感知比较**（期望 > 63 视为 ==63）。
- `tools/check_tb.cpp` 原来**只加载 k=4/k=5 桶**，检查 k≥6 时直接跳过（假 PASS）；改为**通用加载 `min(start,4)..end_k` 全链**。

---

## 5. 最终验证结果（2026-08-18）

### 5.1 结果码：与原表库 100% 一致

`tools/compare_tb` 逐状态比对（0 表示完全一致）：

| 桶 | 条目数 | 结果码差异 | 说明 |
|---|---|---|---|
| k=4 | 33,649,000 | **0** | 原表存在，可比 |
| k=5 | 121,136,400 | **0** | 原表存在，可比 |
| k=6 | 343,219,864 | **0** | 原表存在，可比 |
| k=7 | 784,502,464 | **0** | 原表存在，可比 |
| k=8 | 1,470,942,064 | 无原表 | 仅独立校验 |

距离与原始表差异属预期（原表距离非最优）：k=4 avg −2.98、k=5 −6.27、k=6 −3.76、k=7 平均 |diff| 29。

### 5.2 距离：DTM 最优（独立 minimax 校验）

`tools/check_tb`（逐状态重算理论值）：**k=4、5、6、7、8 全部 0 误差、OPTIMAL**。

### 5.3 性能（80 核机型实测）

| 桶 | 逆推 | 原价值迭代 |
|---|---|---|
| k=4 | ~5s（80 线程） | 101s |
| k=5 | 23s（60 线程） | 610s |
| k=6 | 103s（60 线程） | 3,134s |
| k=7 | 420s（30 线程） | — |
| k=8 | 501s（28 线程） | — |

约 **20 倍加速**（k=4），且距离更优。

---

## 6. 诊断工具

| 工具 | 用途 |
|---|---|
| `tools/check_tb.cpp` | 逐状态 minimax 一致性独立校验（结果 + DTM 距离，任意 k） |
| `tools/compare_tb.cpp` | 两个表库逐状态结果 + 距离比对 |
| `tools/diagnose.cpp` | 打印首个结果差异状态及其后继值 |
| `tools/diagnose2.cpp` | 从首个差异状态做 BFS，打印原表库后继树 |
| `tools/diagnose3.cpp` | 移动表 vs `board.h::gen_moves` 一致性 |
| `tools/diagnose4.cpp` | 单独复现 Phase1/Phase2 init，打印 `cnt`/赋值 |
