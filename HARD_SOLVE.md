# 狼羊棋硬解工程方案（C++）

> 版本：v1.0（2026-08-17）
> 目标：用 C++ 复刻狼羊棋规则，基于**逆向归纳（retrograde analysis）**算法、借助算力对该游戏做**完全数学求解**（strong solving），输出表库（`.tb`）与开局库。
> 方法论来源：某硬解项目作者的做法——**先解狼的最优解，再按羊数 1–15 依次求解；利用棋盘左右镜像压缩计算量**。本方案在其基础上补齐规则口径、状态编码、规模预算与工程落地细节。

---

## 0. 结论先行

1. **游戏可强解**：5×5 有限棋盘、完全信息、无随机、规则终止（150 步/围死/吃尽均终止）→ 局面总数有限，逆向归纳可求得每个局面的博弈论值。
2. **规模可控**：全量局面（含轮次）约 **187.9 亿**；左右镜像去重后约 **93.9 亿**。结果表 2bit/局面 ≈ 2.4 GB，带距离 10bit/局面 ≈ 12 GB——**单机 64GB 内存 + 多核并行即可全量求解**，无需超算。
3. **求解顺序**（作者方法）：先解"狼视角"（狼先手的最优胜负表，按 distance-to-conversion 分层），再按羊数 k = 1 → 15 自底向上逐桶补全"羊视角"。该顺序的数学依据：**吃子是唯一改变羊数的动作且每回合至多一只（k → k−1）**，故 k 桶只依赖 k−1 桶（已解）与桶内局面，依赖无环。
4. **对称性**：经对称群分析，**本游戏只有左右镜像（垂直轴）一个对称操作**（上下/180° 因狼底羊顶的初始布局不对称而不可用），状态量几乎减半。
5. **两个必须预先定死的规则口径**：① 150 步上限 → 表库存"无步数上限的博弈论值 + 到强制胜的距离"，开局时校验距离 ≤ 150；② 走闲规则在最优对局中不出现，可忽略，但"拖平"路径已被 150 步截断覆盖。

---

## 1. 游戏规则形式化（与 `rules.py` 严格一致）

| 项目 | 定义 |
|---|---|
| 棋盘 | 5×5（行 0–4 自上而下，列 0–4 自左而右） |
| 初始局面 | 上三排（行 0–2）15 只羊；第四排空；第五排中间三格 `(4,1)(4,2)(4,3)` 3 只狼 |
| 先行 | 狼 |
| 移动 | 狼/羊每回合向上/下/左/右移动一格，**不可斜走**、不可走入已占格 |
| 吃子 | 狼与羊**恰好隔一个空格**时，可直线跨过该空格落到羊格并吃掉；每回合至多吃 1 只；吃后立即换羊方 |
| 狼胜 | 羊剩余 < 4（即 ≤ 3） |
| 羊胜 | 3 只狼全部无法移动（狼被围死） |
| 平局 | 双方合计走满 150 步；或同一棋子往返走闲双方均达 5 次（后文论证可忽略） |
| 棋子 | 同侧棋子**不可区分**（狼 3 只、羊 k 只各视为同种） |

**硬解判定函数**（每个局面 → 结果）：

```
terminal(state) =
    sheep_count < 4                  → WOLF_WIN
    三狼均无合法移动                  → SHEEP_WIN
    双方合计剩余步数（150 − 已走）耗尽 → DRAW
```

**走闲规则的处理论证**：规则中单方走闲只影响训练扣分，**不直接判负**；判平需"双方"同一棋子往返均达 5 次。最优双方都不会主动走闲（走闲 = 不推进），输方单方走闲无法制造平局（需对方配合）。因此**在"双方最优"假设下，走闲规则不改变胜负判定**；输方唯一可用的拖平手段是走不推进的闲棋，其效果已被 **150 步上限**完全覆盖。→ 硬解口径：忽略走闲细节，仅用 150 步截断。

---

## 2. 总体架构：两阶段逆向归纳

作者方法的工程化表达（与文件名 `wsf_tb_dtc_n*.tb`、`wsf_engine_v7_*` 一致）：

### 2.1 阶段一：狼视角最优解（进攻方完整表）

- 把狼视为进攻方，对**全部局面**（任意狼/羊分布）求解"狼先手时"的结果：WOLF_WIN / SHEEP_WIN / DRAW，并记录**到强制胜的最短步数**。
- 分层方式：**distance-to-conversion（DTC）**——从终局（羊 ≤ 3 → 狼胜）逐层向外剥，每层 = "再走一步/一回合即可进入已解层"的局面。文件名 `dtc_n14 … dtc_n4` 即按此分桶（羊数 14 → 4 的 11 个进攻桶；羊 ≤ 3 为平凡狼胜桶，无需求解）。
- 产物：`狼进攻能力表`——任意局面下狼方最优结果与最快吃子/取胜距离。**这是后续羊视角求解的"已知量"**：狼回合的后继值全部可由本表查出。

### 2.2 阶段二：按羊数 k = 1 → 15 自底向上逐桶补全

- 对每个 k，求解该桶**全部局面**（含"轮到羊走"半桶），得到完整胜负表。
- **递推依据（无环依赖）**：
  - 吃子：狼在 k 桶吃 1 只 → 进入 k−1 桶（**已解**）；
  - 狼非吃子移动：仍在 k 桶（未解，桶内处理）；
  - 羊移动：仍在 k 桶（未解，桶内处理）。
- 桶内求解（两半桶交替不动点，或按距离统一逆推）：

```
solve_bucket(k):
    初始化 k 桶全部状态 = UNKNOWN
    标记终局状态：
        k < 4            → 全部 WOLF_WIN（distance 0）
        三狼无合法移动    → 全部 SHEEP_WIN（distance 0）
    重复直到无变化：
        for 每个狼先手状态 s:
            若 ∃ 走法 → 后继 r ∈ 已解为 WOLF_WIN(距离 d)
               → s = WOLF_WIN(d+1)          # 狼能选到赢
            否则若 全部走法 → 后继均为 SHEEP_WIN
               → s = SHEEP_WIN(max_d+1)
            否则 → 平/未定（继续迭代）
        for 每个羊先手状态 s:                # 对称逻辑
            若 ∃ 走法 → 后继为 SHEEP_WIN → s = SHEEP_WIN
            若 全部走法 → 后继为 WOLF_WIN   → s = WOLF_WIN
```

- 距离按"双方合计步数"累计（与 150 步口径一致）。
- 开局校验：初始局面（k=15、狼先手）的 WOLF_WIN 距离 ≤ 150 → 狼必赢；SHEEP_WIN 距离 ≤ 150 → 羊必赢；否则平。

### 2.3 为什么不从 k=15 往下解、而要从 k=1 往上

k 桶的吃子后继落在 k−1，**向上求解保证依赖已就绪**；且 k 小桶极小（k=1 仅 10 万局面），用于先行验证算法正确性，成本极低。k=1..3 是平凡狼胜桶，实际逆推从 k=4 开始，k=4 桶"狼一步能吃即赢"作为逆推种子。

---

## 3. 左右镜像对称压缩

### 3.1 对称群分析（只有这一个对称可用）

棋盘对称操作候选：左右镜像 M（c → 4−c）、上下翻转（r → 4−r）、180° 旋转（M∘上下）。

- **M 保持一切不变**：初始狼位 `{(4,1),(4,2),(4,3)}` 镜像后仍是自身集合；羊占满行 0–2 镜像后不变；"上下左右移动 + 隔格直吃"规则在镜像下等价。→ **M 是合法对称**。
- **上下翻转不可用**：翻转后狼在行 0、羊在行 2–4，与初始布局（狼底羊顶）不等价；且围死/吃子几何在上下方向不对称。180° 旋转同理不可用。

**结论：对称群 = {e, M}，阶 2。** 任意局面与其左右镜像局面博弈论值完全相同。

### 3.2 工程实现

- **规范化（canonical form）**：编码前取 `key = min(state, mirror(state))`（按紧凑编码比较）。只对规范形分配表库条目。
- **查表**：任意局面先规范化再查；走法生成时若局面非规范形，将镜像走法映射回规范形坐标即可（等价性成立）。
- **自对称局面**：狼位左右对称 且 羊位左右对称的局面 M(s)=s，约占极小比例，天然只存一份；实际条目数 ≈ (N + N_self)/2 ≈ N/2。
- **验证**：求解完成后抽样比对 `value(s) == value(M(s))` 必须全部成立（自检断言）。

### 3.3 收益

| 指标 | 全量 | 镜像去重 |
|---|---|---|
| 局面数（含轮次） | 18,787,536,200 | ≈ 9,393,768,100 |
| 结果表 2bit/局面 | 4.70 GB | 2.35 GB |
| 带距离 10bit/局面 | 23.48 GB | 11.74 GB |

---

## 4. 状态编码与表库格式（`.tb`）

### 4.1 桶划分（按羊数 k，含轮次）

```
k= 1:       101,200      k= 9:  2,288,132,000
k= 2:     1,062,600      k=10:  2,974,571,600
k= 3:     7,084,000      k=11:  3,244,987,200   ← 最大桶
k= 4:    33,649,000      k=12:  2,974,571,600
k= 5:   121,136,400      k=13:  2,288,132,000
k= 6:   343,219,800      k=14:  1,470,942,000
k= 7:   784,502,400      k=15:    784,502,400
k= 8: 1,470,942,000
```

### 4.2 紧凑编码（每个桶内）

- 狼：3 只狼在 25 格中的**组合序**（无序）→ `C(25,3) = 2300` 个取值（12 bit 位打包，或直接用 25bit 位板）；
- 羊：k 只羊在剩余 22 格中的**组合序** → `C(22,k)` 个取值（k=11 时 705,432 → 20 bit）；
- 轮次：1 bit（W/S）；
- **条目值（推荐 10 bit）**：`result(2bit) + distance(8bit)`。result：`00=WOLF_WIN, 01=SHEEP_WIN, 10=DRAW, 11=UNKNOWN`；distance 封顶 255（150 步上限足够，255=∞/未定）。

### 4.3 文件布局（对齐作者命名习惯）

```
data/tb/
├── wsf_tb_dtc_k04_w.bin     # k=4 桶，狼先手半桶
├── wsf_tb_dtc_k04_s.bin     # k=4 桶，羊先手半桶
├── ...                      # k=5..15
└── wsf_tb_dtc_k15_w.bin
```

- 桶内**线性索引**：`idx = wolf_combo_id * C(22,k) * 2 + sheep_combo_id * 2 + turn`，`mmap` 映射，随机访问 O(1)。
- **分桶文件 = 天然断点续算单元**：每桶解完原子落盘，重启后跳过已解桶。
- 元数据头（文件头 64B）：魔数、规则版本、k、狼/羊组合基数、条目位宽、已完成标记、哈希校验。

---

## 5. 算法与算力预算

### 5.1 逆推实现路线（推荐：反向 worklist，避免全表重复扫描）

- 正向 BFS 从初始局面标记**可达集**（25bit 位板 `std::vector<uint64_t>` 可达 bitmap，93.9 亿 bit ≈ 1.2 GB，可选）；
- 从终局（狼胜/羊胜桶）建立 worklist，按距离分层反向传播：
  - 对每个出队状态 s，枚举"上一步"（父状态）：狼走一步/羊走一步/狼吃子（羊被吃后落位回溯）；
  - 父状态按 2.2 的胜负逻辑更新（∃ 赢后继 → 胜；全部败后继 → 败），更新后入队；
  - 距离 = 子距离 + 1（双方合计）。
- **总量**：每状态平均分支 ~8–15，总操作 ≈ 94 亿 × 10 ≈ **1e12 量级**。
- **单机预算**：现代 CPU 单核 ~2–5 亿局面/分钟级处理；32 核 OpenMP 并行 + 桶间并行（k 桶相互独立时）→ **数小时 ~ 十小时级**全解。内存：10bit/状态 × 94 亿 ≈ **11.7 GB**（mmap，64GB 机器宽裕）。

### 5.2 并行化

| 层级 | 方式 |
|---|---|
| 桶间 | k 桶按依赖顺序（k=4→15）串行（k 依赖 k−1），但**桶内狼组合块**完全独立 → 分块并行 |
| 桶内 | 按 `wolf_combo_id` 分 N 块，OpenMP / std::thread 并行逆推，每块独立内存区 |
| 多机（可选） | 桶文件 + 块文件分发，`worker` 进程 + 结果回传合并 |

### 5.3 可达性优化（二期）

初始局面羊全在上三排，**k 大的桶大量羊组合不可达**（羊需逐格游走）。先做一次正向 BFS 统计可达数再决定是否做稀疏表（`unordered_map<uint64_t, uint8_t>`）。一期建议直接全量线性表（简单、可验证），二期用可达集压缩。

---

## 6. C++ 工程规划

### 6.1 目录结构

```
hard_solve/
├── CMakeLists.txt
├── include/
│   ├── board.h        # 局面表示、走法生成、胜负判定（与 rules.py 对拍）
│   ├── symmetry.h     # 左右镜像 + canonical 规范化
│   ├── encode.h       # 组合编码/解码（狼 C(25,3)、羊 C(22,k)）
│   ├── tablebase.h    # .tb 读写（mmap、元数据头、断点）
│   └── solver.h       # 逆向归纳求解器（worklist）
├── src/
│   ├── board.cpp
│   ├── symmetry.cpp
│   ├── encode.cpp
│   ├── tablebase.cpp
│   └── solver.cpp
├── tools/
│   ├── solve_all.cpp        # 主求解入口（分桶、并行、断点续算）
│   ├── dump_opening_book.cpp # 导出开局库 JSON（初始局面→首手/结果）
│   ├── selfcheck.cpp        # 对称一致性 + 终局判定自检
│   └── verify.cpp           # 随机局面结果验证（可对拍 Python）
├── tests/
│   └── crosscheck/          # 与 Python rules.py 走法集对拍
└── data/tb/                 # 输出表库
```

### 6.2 关键模块要点

- **board.h**：`struct State { uint64_t sheep; uint64_t wolf; uint8_t turn; }`（25bit 位板 ×2 + 1bit），`gen_moves()`、`apply()`、`is_terminal()`。**必须与 `rules.py` 的 `legal_moves_from` / `_update_winner` 逐条等价**（对拍见 §7）。
- **symmetry.h**：`canonical(s)` 返回 `min(enc(s), enc(M(s)))`；`mirror_move()` 坐标映射。
- **encode.h**：组合序编码用"二项式系数组合编号"（Lehmer 逆）双向转换，编译期生成组合计数表。
- **solver.cpp**：worklist 逆推 + 距离分层；`std::vector<uint8_t>` 或 `mmap` 表；`std::atomic` 分块进度；桶级 checkpoint（每桶完成后写元数据头完成标记）。
- **dump_opening_book.cpp**：解完后输出 JSON（与作者 `opening book.json` 同构）：

```json
{ "fen": "5s5s5s/5/3w3w3w", "turn": "wolf",
  "result": "wolf_win", "distance": 87,
  "best_move": [["4,1","3,1"]], "mirror_ok": true }
```

### 6.3 构建

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
# 建议：-O3 -march=native -flto -fopenmp；Apple Silicon 可加 -mcpu=apple-m1-max
```

---

## 7. 正确性验证策略（先小后大，逐级放行）

1. **走法对拍**（零容忍）：随机抽样 10 万局面，C++ `gen_moves()` 输出集与 Python `rules.py::legal_moves_from` **完全一致**（工具：dump 局面 FEN → 两边各产走法 JSON → diff）。
2. **终局判定对拍**：狼胜/羊胜/平局三判据逐条等价。
3. **小规模先行**：只解 k ≤ 6 桶（约 5 亿局面/镜像后 2.5 亿），验证：终局种子正确、距离单调、镜像值一致、`k` 桶与 `k−1` 桶递推闭合。
4. **对称自检**：随机局面 `value(s) == value(M(s))` 全通过。
5. **开局一致性**：初始局面结果与 `train_ai.py` / 自对弈观测交叉印证（如已知狼先手对随机羊 100% 胜，硬解应给出狼胜且距离有限）。
6. **与作者表库互验（可选，推荐）**：请作者从 `wsf_tb_dtc_n*.tb` 导出文本（`fen, turn, result, distance, best_move`），同局面逐条比对。若一致，双方实现互相背书。
7. **最终交付**：开局库 JSON 覆盖初始局面全部首手（含镜像对），每条含 best_move 与 distance。

---

## 8. 风险与开放问题

| # | 风险 | 影响 | 对策 |
|---|---|---|---|
| 1 | **150 步平局口径**：表库存"无步数上限博弈论值"，开局时若强制胜距离 > 150 实际判平 | 开局结论可能误判 | 距离必须编码进表库；开局单独校验 ≤ 150 |
| 2 | **走闲规则**严格实现会让状态携带往返计数 → 状态爆炸 | 不可行 | 论证后忽略（§1），以 150 步截断覆盖拖平 |
| 3 | 全量 94 亿局面是**上界**，k 大桶大量不可达 | 浪费算力/内存 | 二期可达集过滤（§5.3） |
| 4 | 组合编码/镜像映射实现 bug 难肉眼发现 | 表库静默错误 | 全套对拍 + 自检 + 小规模先行 |
| 5 | 作者 `.tb` 为私有二进制，无法直接读 | 无法直接复用其解 | 仅做"文本导出 → 互验"合作，不依赖其格式 |
| 6 | 算力估算（数小时–十小时级）含较大常数不确定 | 工期不确定 | 先跑 k≤6 桶实测单桶耗时，外推全量 |

**关键开放问题**：作者"先解狼的最优解"与"dtc n4–n14"桶的对应关系，建议直接向其确认两点——① DTC 的"conversion"指"吃到下一只羊"还是"达成最终胜利"；② 桶编号 n 是否 = 羊数 k。确认后本方案的阶段一/二与表库分桶即可与其逐项对齐。

---

## 9. 实施路线图（建议）

```
里程碑 M0：C++ 规则引擎 + 走法/终局对拍通过          （1–2 天）
里程碑 M1：编码 + 镜像规范化 + 自检通过               （1 天）
里程碑 M2：worklist 逆推求解器，解完 k≤6 桶并验证     （2–3 天）
里程碑 M3：全量分桶求解（k=4→15），断点续算          （数小时–十小时，实测外推）
里程碑 M4：开局库导出 + 交叉验证 + 与作者互验         （1 天）
```

---

## 附录 A：规模数字来源

```
C(25,3) = 2300                       # 3 狼落位组合
Σ_{k=1..15} C(22,k) = 4,084,247      # 1..15 只羊落位组合（22 = 25−3 空格位）
无轮次局面 = 2300 × 4,084,247 = 9,393,768,100
含轮次     = 9,393,768,100 × 2 = 18,787,536,200
镜像去重   ≈ 18,787,536,200 / 2 ≈ 9,393,768,100
2bit 表    = 9,393,768,100 × 2 / 8 ≈ 2.35 GB
10bit 表   = 9,393,768,100 × 10 / 8 ≈ 11.74 GB
```
